<%= render partial: 'pages/header', :locals => { text: "Tickets list" } %>

<% if can? :create, Ticket %>
<%= link_to 'New ticket', new_ticket_path, :class=>'btn btn-default' %>
<% end %>
<div class="section-space"><!-- main container -->
  <div class="row">
      <div class="table-responsive">
      <div class="col-md-12 col-sm-12">

    <%= render partial: 'shared/ticket', :locals => { tickets: @tickets } unless @tickets.blank? %>
  </div>
  </div>
</div>
</div>
